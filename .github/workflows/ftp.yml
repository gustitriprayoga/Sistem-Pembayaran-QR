on: push
name: 🚀 Deploy website on push
jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Upload Files
      uses: pontago/lftp-deploy-action@master
      with:
        server: ftp.kodai.id
        username: tuan-coffee@tuan-coffee.laratest.my.id
        password: ${{ secrets.ftp_password }}
